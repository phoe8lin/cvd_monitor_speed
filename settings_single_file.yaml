proxy:
  host: ""  # 空字符串表示不使用代理
  port: 0
  type: "http"

data_saving:
  interval_minutes: 1  # 1分钟保存一次
  use_single_file: true  # 使用单文件模式

cvd_reset:
  max_age_days: 1

config_reload:
  check_interval_seconds: 60
